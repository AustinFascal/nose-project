
  # Nosè (Node Safe) - High Fidelity

  This is a code bundle for Nosè (Node Safe) - High Fidelity. The original project is available at https://www.figma.com/design/94FxDIF4DLNeXRou1yMbXd/Nos%C3%A8--Node-Safe----High-Fidelity.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  